# Core RAG stack - LangChain 0.3+ modular packages
langchain-core>=0.3.0            # base interfaces: prompts, runnables, parsers
langchain-community>=0.3.0       # document loaders, ChromaDB vectorstore
langchain-groq>=0.2.0            # ChatGroq - Groq API (llama, gemma, mixtral)
langchain-ollama>=0.2.0          # ChatOllama - local LLM via Ollama (fully offline)
langchain-huggingface>=0.1.0     # HuggingFaceEmbeddings (replaces deprecated langchain-community version)
langchain-text-splitters>=0.3.0  # RecursiveCharacterTextSplitter

# Vector store
chromadb>=0.5.0

# Document loaders
pypdf>=4.2.0          # PDF support (used by PyPDFLoader)
docx2txt>=0.8         # DOCX support (used by Docx2txtLoader)
beautifulsoup4>=4.12.0  # web page ingestion (used by WebBaseLoader)
lxml>=4.9.0           # HTML parser for BeautifulSoup
pandas>=2.1.0         # CSV / Excel parsing
openpyxl>=3.1.0       # Excel .xlsx support for pandas

# Local embeddings (no API key needed - runs on CPU)
sentence-transformers>=3.0.0

# BM25 keyword search for hybrid retrieval
rank-bm25>=0.2.2

# Token counting (still needed by langchain-openai internals)
tiktoken>=0.7.0

# Backend API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9   # required by FastAPI for file uploads

# Utilities
python-dotenv>=1.0.1  # Load .env file
